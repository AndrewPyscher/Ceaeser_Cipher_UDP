HW2 README:

Change line 41 in the Client to be the servers IP address if running on 2 machines.
If running on one, change it to "localhost"

Start the Server and the Client programs.
Enter the users first name in all caps, then the last name in all caps
then it will return the matching SSN if its in the file.

To add a new record to the file, add it to the Sample.txt file